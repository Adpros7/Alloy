/* Intentionally empty.
 *
 * SwiftPM needs at least one compilable source for a C target. All declarations
 * live in include/alloy_engine.h; the implementations are linked from the Rust
 * static library liballoy_text.a (see the executable target's linkerSettings).
 */
